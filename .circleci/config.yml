version: 2
jobs:
  install-dependencies:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn
      - run: echo "Checking if lockfiles changed..." && git diff --exit-code
      - save_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
          paths:
            - node_modules
      - run: npm rebuild node-sass

  compile-libs:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn compile-libs
      - persist_to_workspace:
          root: '.'
          paths:
            - yarn.lock
            - packages/core/dist
            - packages/datetime/dist
            - packages/docs/dist
            - packages/labs/dist
            - packages/table/dist

  lint:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn lint

  dist:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: yarn dist
      - persist_to_workspace:
          root: '.'
          paths:
            - yarn.lock
            - packages/core/dist
            - packages/datetime/dist
            - packages/docs/dist
            - packages/labs/dist
            - packages/table/dist

  test-core:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./node_modules/.bin/lerna run test --scope '@blueprintjs/core'

  test-datetime:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./node_modules/.bin/lerna run test --scope '@blueprintjs/datetime'

  test-labs:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./node_modules/.bin/lerna run test --scope '@blueprintjs/labs'

  test-table:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./node_modules/.bin/lerna run test --scope '@blueprintjs/table'

  deploy-preview:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./scripts/submit-preview-comment

  deploy-npm:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - attach_workspace:
          at: '.'
      - restore_cache:
          key: v1-dependency-cache-{{ checksum "yarn.lock" }}
      - run: ./scripts/publish-npm-semver-tagged

workflows:
  version: 2
  compile_lint_test_dist_deploy:
    jobs:
      - install-dependencies
      - compile-libs:
          requires: [install-dependencies]
      - lint:
          requires: [install-dependencies]
      - dist:
          requires: [compile-libs]
      - test-core:
          requires: [compile-libs]
      - test-datetime:
          requires: [compile-libs]
      - test-labs:
          requires: [compile-libs]
      - test-table:
          requires: [compile-libs]
      - deploy-preview:
          requires:
            - lint
            - dist
            - test-core
            - test-datetime
            - test-labs
            - test-table
      - deploy-npm:
          requires: [deploy-preview]
          filters:
            tags:
              only:
                - /^release-.*/
            branches:
              ignore:
                - /.*/
