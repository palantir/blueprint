type: fix
fix:
  description: "[MultiSelect] `tagInputProps.onAdd` is now forwarded to the underlying `TagInput` instead of being overwritten"
  links:
  - https://github.com/palantir/blueprint/pull/8144
