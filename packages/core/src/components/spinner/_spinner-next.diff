diff --git a/packages/core/src/components/spinner/_spinner.scss b/packages/core/src/components/spinner/_spinner-next.scss
index 988e01564..172c11b01 100644
--- a/packages/core/src/components/spinner/_spinner.scss
+++ b/packages/core/src/components/spinner/_spinner-next.scss
@@ -1,10 +1,9 @@
-// Copyright 2015 Palantir Technologies, Inc. All rights reserved.
+// Copyright 2026 Palantir Technologies, Inc. All rights reserved.
 // Licensed under the Apache License, Version 2.0.
 
 @import "../../common/variables";
-@import "../progress-bar/common";
 
-@keyframes pt-spinner-animation {
+@keyframes pt-spinner-animation-next {
   from {
     transform: rotate(0deg);
   }
@@ -14,13 +13,13 @@
   }
 }
 
-.#{$ns}-spinner:not(:where(.bp-next .#{$ns}-spinner)) {
+.#{$ns}-spinner {
   align-items: center;
-  // center animation container inside parent element to isolate layout
+  // Center animation container inside parent element to isolate layout.
   display: flex;
   justify-content: center;
 
-  // allow paths to overflow container -- critical for edges of circles!
+  // Allow paths to overflow container -- critical for edges of circles.
   overflow: visible;
   vertical-align: middle;
 
@@ -33,39 +32,30 @@
   }
 
   .#{$ns}-spinner-head {
-    stroke: $progress-head-color;
+    stroke: var(--bp-typography-color-intent-neutral);
     stroke-linecap: round;
     transform-origin: center;
-    transition: stroke-dashoffset ($pt-transition-duration * 2) $pt-transition-ease;
+    transition: stroke-dashoffset calc(var(--bp-emphasis-transition-duration) * 2)
+      var(--bp-emphasis-ease-default);
   }
 
   .#{$ns}-spinner-track {
-    stroke: $progress-track-color;
+    stroke: var(--bp-surface-layer-color-neutral-rest);
   }
 }
 
-// put the animation on a child HTML element to isolate it from display of parent
-.#{$ns}-spinner-animation:not(:where(.bp-next .#{$ns}-spinner-animation)) {
-  animation: pt-spinner-animation ($pt-transition-duration * 5) linear infinite;
+// Put the animation on a child HTML element to isolate it from display of parent.
+.#{$ns}-spinner-animation {
+  animation: pt-spinner-animation-next calc(var(--bp-emphasis-transition-duration) * 5) linear
+    infinite;
 
   .#{$ns}-no-spin > & {
     animation: none;
   }
 }
 
-.#{$ns}-dark .#{$ns}-spinner:not(:where(.bp-next .#{$ns}-spinner)) {
-  .#{$ns}-spinner-head {
-    stroke: $dark-progress-head-color;
-  }
-
-  .#{$ns}-spinner-track {
-    stroke: $dark-progress-track-color;
-  }
-}
-
-@each $intent, $color in $pt-intent-colors {
-  .#{$ns}-spinner.#{$ns}-intent-#{$intent}:not(:where(.bp-next .#{$ns}-spinner))
-    .#{$ns}-spinner-head {
-    stroke: $color;
+@each $intent in $pt-intents {
+  .#{$ns}-spinner.#{$ns}-intent-#{$intent} .#{$ns}-spinner-head {
+    stroke: var(--bp-typography-color-intent-#{$intent});
   }
 }
