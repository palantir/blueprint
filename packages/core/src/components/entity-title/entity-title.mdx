---
title: Entity Title
---

# Entity Title

**EntityTitle** is a component that handles rendering a common UI pattern consisting of title, icon, subtitle and tag.

## Usage

```ts copy
import { EntityTitle } from "@blueprintjs/core";
```

```tsx
<EntityTitle icon={IconNames.INFO_SIGN} title="Announcement" subtitle={"Announcement details"} />
```

## Examples

### Basic

@reactCodeExample EntityTitleBasicExample

### Title URL

The `titleURL` prop wraps the title text in an anchor element, turning it into a clickable link.

@reactCodeExample EntityTitleTitleUrlExample

### Loading

The `loading` prop renders the component in a skeleton loading state.

@reactCodeExample EntityTitleLoadingExample

### Fill

The `fill` prop allows the component to expand and fill the available space in its container.

@reactCodeExample EntityTitleFillExample

### Subtitle

The `subtitle` prop renders muted text below the title.

@reactCodeExample EntityTitleSubtitleExample

### Tags

The `tags` prop renders tag components alongside the title. `<Tag>` components work best.

@reactCodeExample EntityTitleTagsExample

@## Interactive Playground

@reactExample EntityTitleExample

## Props interface

@interface EntityTitleProps
