diff --git a/packages/core/src/components/tabs/_tabs.scss b/packages/core/src/components/tabs/_tabs-next.scss
index d5072651c..367054e80 100644
--- a/packages/core/src/components/tabs/_tabs.scss
+++ b/packages/core/src/components/tabs/_tabs-next.scss
@@ -4,12 +4,7 @@
 @import "../../common/variables";
 @import "../../common/mixins";
 
-$tab-color-selected: $pt-link-color !default;
-$dark-tab-color-selected: $pt-dark-link-color !default;
-
-$tab-indicator-width: $pt-spacing * 0.75 !default;
-
-.#{$ns}-tabs:not(.#{$ns}-vertical).#{$ns}-fill:not(:where(.bp-next .#{$ns}-tabs)) {
+.#{$ns}-tabs:not(.#{$ns}-vertical).#{$ns}-fill {
   height: 100%;
 
   .#{$ns}-tab-list {
@@ -17,7 +12,7 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
   }
 }
 
-.#{$ns}-tabs.#{$ns}-vertical:not(:where(.bp-next .#{$ns}-tabs)) {
+.#{$ns}-tabs.#{$ns}-vertical {
   display: flex;
 
   // include '>' to ensure we're only modifying
@@ -29,25 +24,26 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
 
     .#{$ns}-tab {
       align-items: center;
-      border-radius: $pt-border-radius;
+      border-radius: var(--bp-surface-border-radius);
       display: flex;
-      padding: 0 ($pt-spacing * 2);
+      padding-block: 0;
+      padding-inline: calc(var(--bp-surface-spacing) * 2);
       width: 100%;
 
       &[aria-selected="true"] {
-        background-color: rgba($pt-intent-primary, 0.2);
+        background-color: var(--bp-surface-layer-color-primary-rest);
         box-shadow: none;
 
         @media (forced-colors: active) and (prefers-color-scheme: dark) {
           background-color: $pt-high-contrast-mode-active-background-color;
-          color: $black;
+          color: CanvasText;
         }
       }
     }
 
     .#{$ns}-tab-indicator-wrapper .#{$ns}-tab-indicator {
-      background-color: rgba($pt-intent-primary, 0.2);
-      border-radius: $pt-border-radius;
+      background-color: var(--bp-surface-layer-color-primary-rest);
+      border-radius: var(--bp-surface-border-radius);
       height: auto;
       inset: 0;
     }
@@ -58,14 +54,14 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
   // vertical tab component
   > .#{$ns}-tab-panel {
     margin-top: 0;
-    padding-left: $pt-spacing * 5;
+    padding-inline-start: calc(var(--bp-surface-spacing) * 5);
   }
 }
 
-.#{$ns}-tab-list:not(:where(.bp-next .#{$ns}-tab-list)) {
+.#{$ns}-tab-list {
   align-items: flex-end;
   border: none;
-  column-gap: $pt-spacing * 5;
+  column-gap: calc(var(--bp-surface-spacing) * 5);
   display: flex;
   flex: 0 0 auto;
   list-style: none;
@@ -77,20 +73,20 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
     // offset column-gap from parent component above
     // since flex-expander is taking up _available_ space, we don't want to add a double gap
     // between the elements before and after the expander
-    margin-right: $pt-spacing * -5;
+    margin-inline-end: calc(var(--bp-surface-spacing) * -5);
   }
 }
 
-.#{$ns}-tab:not(:where(.bp-next .#{$ns}-tab)) {
+.#{$ns}-tab {
   @include overflow-ellipsis;
   align-items: center;
   align-self: stretch;
-  color: $pt-text-color;
+  color: var(--bp-typography-color-base);
   cursor: pointer;
   display: flex;
   flex: 0 0 auto;
-  font-size: $pt-font-size;
-  line-height: $pt-button-height;
+  font-size: var(--bp-typography-size-md);
+  line-height: calc(var(--bp-surface-spacing) * 7.5);
   max-width: 100%;
   position: relative;
   vertical-align: top;
@@ -112,7 +108,7 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
   }
 
   &[aria-disabled="true"] {
-    color: $pt-text-color-disabled;
+    color: oklch(from var(--bp-typography-color-base) l c h / var(--bp-emphasis-disabled-opacity));
     cursor: not-allowed;
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
@@ -123,12 +119,13 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
 
   &[aria-selected="true"] {
     border-radius: 0;
-    box-shadow: inset 0 (-$tab-indicator-width) 0 $tab-color-selected;
+    box-shadow: inset 0 calc(var(--bp-surface-spacing) * -0.75) 0
+      var(--bp-typography-color-intent-primary);
   }
 
   &[aria-selected="true"],
   &:not([aria-disabled="true"]):hover {
-    color: $tab-color-selected;
+    color: var(--bp-typography-color-intent-primary);
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
       // Windows High Contrast dark theme
@@ -141,41 +138,41 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
   }
 
   .#{$ns}-large > & {
-    font-size: $pt-font-size-large;
-    line-height: $pt-button-height-large;
+    font-size: var(--bp-typography-size-lg);
+    line-height: calc(var(--bp-surface-spacing) * 10);
   }
 }
 
-.#{$ns}-tab-panel:not(:where(.bp-next .#{$ns}-tab-panel)) {
-  margin-top: $pt-spacing * 5;
+.#{$ns}-tab-panel {
+  margin-block-start: calc(var(--bp-surface-spacing) * 5);
 
   &[aria-hidden="true"] {
     display: none;
   }
 }
 
-.#{$ns}-tab-icon:not(:where(.bp-next .#{$ns}-tab-icon)) {
-  margin-right: $pt-spacing * 2;
+.#{$ns}-tab-icon {
+  margin-inline-end: calc(var(--bp-surface-spacing) * 2);
 }
 
-.#{$ns}-tab-tag:not(:where(.bp-next .#{$ns}-tab-tag)) {
-  margin-left: $pt-spacing * 2;
+.#{$ns}-tab-tag {
+  margin-inline-start: calc(var(--bp-surface-spacing) * 2);
 }
 
-.#{$ns}-tab-indicator-wrapper:not(:where(.bp-next .#{$ns}-tab-indicator-wrapper)) {
+.#{$ns}-tab-indicator-wrapper {
   left: 0;
   pointer-events: none;
   position: absolute;
   top: 0;
   transform: translateX(0), translateY(0);
-  transition: height, transform, width;
-  transition-duration: $pt-transition-duration * 2;
-  transition-timing-function: $pt-transition-ease;
+  transition-duration: calc(var(--bp-emphasis-transition-duration) * 2);
+  transition-property: height, transform, width;
+  transition-timing-function: var(--bp-emphasis-ease-default);
 
   .#{$ns}-tab-indicator {
-    background-color: $tab-color-selected;
+    background-color: var(--bp-typography-color-intent-primary);
     bottom: 0;
-    height: $tab-indicator-width;
+    height: calc(var(--bp-surface-spacing) * 0.75);
     left: 0;
     position: absolute;
     right: 0;
@@ -195,44 +192,6 @@ $tab-indicator-width: $pt-spacing * 0.75 !default;
   }
 }
 
-.#{$ns}-dark {
-  .#{$ns}-tab:not(:where(.bp-next .#{$ns}-tab)) {
-    color: $pt-dark-text-color;
-
-    &[aria-disabled="true"] {
-      color: $pt-dark-text-color-disabled;
-
-      @media (forced-colors: active) and (prefers-color-scheme: dark) {
-        // Windows High Contrast dark theme
-        color: $pt-high-contrast-mode-disabled-text-color;
-      }
-    }
-
-    &[aria-selected="true"] {
-      box-shadow: inset 0 (-$tab-indicator-width) 0 $dark-tab-color-selected;
-    }
-
-    &[aria-selected="true"],
-    &:not([aria-disabled="true"]):hover {
-      color: $dark-tab-color-selected;
-
-      @media (forced-colors: active) and (prefers-color-scheme: dark) {
-        // Windows High Contrast dark theme
-        color: $pt-high-contrast-mode-active-text-color;
-      }
-    }
-  }
-
-  .#{$ns}-tab-indicator:not(:where(.bp-next .#{$ns}-tab-indicator)) {
-    background-color: $dark-tab-color-selected;
-
-    @media (forced-colors: active) and (prefers-color-scheme: dark) {
-      // Windows High Contrast dark theme
-      background-color: $pt-high-contrast-mode-active-background-color;
-    }
-  }
-}
-
-.#{$ns}-flex-expander:not(:where(.bp-next .#{$ns}-flex-expander)) {
+.#{$ns}-flex-expander {
   flex: 1 1;
 }
