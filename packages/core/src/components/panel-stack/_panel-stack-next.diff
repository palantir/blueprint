diff --git a/packages/core/src/components/panel-stack/_panel-stack.scss b/packages/core/src/components/panel-stack/_panel-stack-next.scss
index 7dc87241e..b64817b0e 100644
--- a/packages/core/src/components/panel-stack/_panel-stack.scss
+++ b/packages/core/src/components/panel-stack/_panel-stack-next.scss
@@ -3,25 +3,20 @@
 
 @import "../../common/variables";
 @import "../../common/react-transition";
-@import "../card/card-variables";
 
-.#{$ns}-panel-stack2:not(:where(.bp-next .#{$ns}-panel-stack2)) {
+.#{$ns}-panel-stack2 {
   overflow: hidden;
   position: relative;
 }
 
-.#{$ns}-panel-stack2-header:not(:where(.bp-next .#{$ns}-panel-stack2-header)) {
+.#{$ns}-panel-stack2-header {
   align-items: center;
-  box-shadow: 0 1px $pt-divider-black;
+  box-shadow: 0 var(--bp-surface-border-width) var(--bp-surface-divider-color-default);
   display: flex;
   flex-shrink: 0;
-  height: $pt-spacing * 7.5;
+  height: calc(var(--bp-surface-spacing) * 7.5);
   z-index: 1;
 
-  .#{$ns}-dark & {
-    box-shadow: 0 1px $pt-dark-divider-white;
-  }
-
   // two span children act as spacers to keep the title centered.
   > span {
     align-items: stretch;
@@ -30,47 +25,43 @@
   }
 
   .#{$ns}-heading {
-    margin: 0 $pt-spacing;
+    margin-block: 0;
+    margin-inline: var(--bp-surface-spacing);
   }
 }
 
-.#{$ns}-button.#{$ns}-panel-stack2-header-back:not(
-    :where(.bp-next .#{$ns}-panel-stack2-header-back)
-  ) {
-  margin-left: $pt-spacing;
-  padding-left: 0;
+.#{$ns}-button.#{$ns}-panel-stack2-header-back {
+  margin-inline-start: var(--bp-surface-spacing);
+  padding-inline-start: 0;
   white-space: nowrap;
 
   .#{$ns}-icon {
     // reduce margins around icon so it fits better in tight header
-    margin: 0 ($pt-spacing * 0.5);
+    margin-block: 0;
+    margin-inline: calc(var(--bp-surface-spacing) * 0.5);
   }
 }
 
-.#{$ns}-panel-stack2-view:not(:where(.bp-next .#{$ns}-panel-stack2-view)) {
+.#{$ns}-panel-stack2-view {
   @include position-all(absolute, 0);
 
-  background-color: $card-background-color;
-  border-right: 1px solid $pt-divider-black;
+  background-color: var(--bp-surface-background-color-base-rest);
+  border-inline-end: var(--bp-surface-border-width) solid var(--bp-surface-divider-color-default);
   display: flex;
   flex-direction: column;
 
   // border between panels, visible during transition
-  margin-right: -1px;
+  margin-inline-end: calc(-1 * var(--bp-surface-border-width));
   overflow-y: auto;
   z-index: 1;
 
-  .#{$ns}-dark & {
-    background-color: $dark-card-background-color;
-  }
-
   &:nth-last-child(n + 4) {
     display: none;
   }
 }
 
 // PUSH transition: enter from right (100%), existing panel moves off left.
-.#{$ns}-panel-stack2-push:not(:where(.bp-next .#{$ns}-panel-stack2-push)) {
+.#{$ns}-panel-stack2-push {
   @include react-transition-phase(
     "#{$ns}-panel-stack2",
     "enter",
@@ -78,8 +69,8 @@
       transform: translateX(100%) translate(0%),
       opacity: 0 1,
     ),
-    $easing: ease,
-    $duration: $pt-transition-duration * 4
+    $easing: var(--bp-emphasis-ease-default),
+    $duration: calc(var(--bp-emphasis-transition-duration) * 4)
   );
   @include react-transition-phase(
     "#{$ns}-panel-stack2",
@@ -88,13 +79,13 @@
       transform: translateX(-50%) translate(0%),
       opacity: 0 1,
     ),
-    $easing: ease,
-    $duration: $pt-transition-duration * 4
+    $easing: var(--bp-emphasis-ease-default),
+    $duration: calc(var(--bp-emphasis-transition-duration) * 4)
   );
 }
 
 // POP transition: enter from left (-50%), existing panel moves off right.
-.#{$ns}-panel-stack2-pop:not(:where(.bp-next .#{$ns}-panel-stack2-pop)) {
+.#{$ns}-panel-stack2-pop {
   @include react-transition-phase(
     "#{$ns}-panel-stack2",
     "enter",
@@ -102,8 +93,8 @@
       transform: translateX(-50%) translate(0%),
       opacity: 0 1,
     ),
-    $easing: ease,
-    $duration: $pt-transition-duration * 4
+    $easing: var(--bp-emphasis-ease-default),
+    $duration: calc(var(--bp-emphasis-transition-duration) * 4)
   );
   @include react-transition-phase(
     "#{$ns}-panel-stack2",
@@ -112,7 +103,7 @@
       transform: translateX(100%) translate(0%),
       opacity: 0 1,
     ),
-    $easing: ease,
-    $duration: $pt-transition-duration * 4
+    $easing: var(--bp-emphasis-ease-default),
+    $duration: calc(var(--bp-emphasis-transition-duration) * 4)
   );
 }
