diff --git a/packages/core/src/components/skeleton/_skeleton.scss b/packages/core/src/components/skeleton/_skeleton-next.scss
index 7bbfb039d..d3ba170ea 100644
--- a/packages/core/src/components/skeleton/_skeleton.scss
+++ b/packages/core/src/components/skeleton/_skeleton-next.scss
@@ -1,60 +1,43 @@
-// Copyright 2016 Palantir Technologies, Inc. All rights reserved.
+// Copyright 2026 Palantir Technologies, Inc. All rights reserved.
 // Licensed under the Apache License, Version 2.0.
 
-@import "./common";
+@import "../../common/variables";
 
-/*
-Skeletons
-
-Markup:
-<div class="#{$ns}-card">
-  <h5 class="#{$ns}-heading"><a class="{{.modifier}}" href="#" tabindex="-1">Card heading</a></h5>
-  <p class="{{.modifier}}">
-    Lorem ipsum dolor sit amet, consectetur adipiscing elit. Quisque eget tortor felis.
-    Fusce dapibus metus in dapibus mollis. Quisque eget ex diam.
-  </p>
-  <button type="button" class="#{$ns}-button #{$ns}-icon-add {{.modifier}}" tabindex="-1">Submit</button>
-</div>
-
-.#{$ns}-skeleton - Render this element as a skeleton, an outline of its true self.
-
-Styleguide skeleton
-*/
-
-@keyframes skeleton-glow {
+@keyframes skeleton-glow-next {
   from {
-    background: $skeleton-color-start;
-    border-color: $skeleton-color-start;
+    background: var(--bp-surface-layer-neutral-rest);
+    border-color: var(--bp-surface-layer-color-neutral-rest);
   }
 
   to {
-    background: $skeleton-color-end;
-    border-color: $skeleton-color-end;
+    background: var(--bp-surface-layer-neutral-hover);
+    border-color: var(--bp-surface-layer-color-neutral-hover);
   }
 }
 
 // This class hides content with a glowing, rounded rectangle.
 // CSS properties that should always override consumer values use the "!important" rule.
 /* stylelint-disable declaration-no-important */
-.#{$ns}-skeleton:not(:where(.bp-next .#{$ns}-skeleton)) {
-  animation: $skeleton-animation;
+.#{$ns}-skeleton {
+  animation: skeleton-glow-next calc(var(--bp-emphasis-transition-duration) * 10) linear infinite
+    alternate;
 
-  // do not !important this for Firefox support
-  background: $skeleton-color-start;
+  // Do not !important this for Firefox support.
+  background: var(--bp-surface-layer-neutral-rest);
 
-  // Prevent background color from extending to the border and overlappping
+  // Prevent background color from extending to the border and overlapping.
   background-clip: padding-box !important;
-  border-color: $skeleton-color-start !important;
-  border-radius: $pt-spacing * 0.5;
+  border-color: var(--bp-surface-layer-color-neutral-rest) !important;
+  border-radius: calc(var(--bp-surface-spacing) * 0.5);
   box-shadow: none !important;
 
-  // Transparent text will occupy space but be invisible to the user
+  // Transparent text will occupy space but be invisible to the user.
   color: transparent !important;
   cursor: default;
   pointer-events: none;
   user-select: none;
 
-  // Make pseudo-elements (CSS icons) and children invisible
+  // Make pseudo-elements (CSS icons) and children invisible.
   &::before,
   &::after,
   * {
