diff --git a/packages/core/src/components/drawer/_drawer.scss b/packages/core/src/components/drawer/_drawer-next.scss
index 35fdb9714..3e7c37770 100644
--- a/packages/core/src/components/drawer/_drawer.scss
+++ b/packages/core/src/components/drawer/_drawer-next.scss
@@ -1,22 +1,14 @@
-// Copyright 2018 Palantir Technologies, Inc. All rights reserved.
+// Copyright 2026 Palantir Technologies, Inc. All rights reserved.
 // Licensed under the Apache License, Version 2.0.
 
-@import "@blueprintjs/icons/lib/scss/variables";
 @import "../../common/mixins";
 @import "../../common/react-transition";
-@import "../../common/variables";
+@import "drawer-variables-next";
 
-$drawer-margin: ($pt-spacing * 7.5) 0 !default;
-$drawer-padding: $pt-spacing * 5 !default;
-
-$drawer-default-size: 50%;
-
-$drawer-background-color: $white !default;
-$dark-drawer-background-color: $dark-gray3 !default;
-
-.#{$ns}-drawer:not(:where(.bp-next .#{$ns}-drawer)) {
-  background: $drawer-background-color;
-  box-shadow: $pt-elevation-shadow-4;
+.#{$ns}-drawer {
+  background: $drawer-background-color-next;
+  box-shadow: var(--bp-surface-shadow-4);
+  color: var(--bp-typography-color-base);
   display: flex;
   flex-direction: column;
   margin: 0;
@@ -26,6 +18,7 @@ $dark-drawer-background-color: $dark-gray3 !default;
     outline: 0;
   }
 
+  // Preserve the legacy defaults: 200ms enter and 100ms exit from every edge.
   &.#{$ns}-position-top {
     @include react-transition-phase(
       "#{$ns}-overlay",
@@ -36,8 +29,8 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateY(0),
         ),
       ),
-      $pt-transition-duration * 2,
-      $pt-transition-ease,
+      calc(var(--bp-emphasis-transition-duration) * 2),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     @include react-transition-phase(
@@ -49,20 +42,19 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateY(0),
         ),
       ),
-      $pt-transition-duration,
+      var(--bp-emphasis-transition-duration),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
-    height: $drawer-default-size;
+    height: $drawer-default-size-next;
     left: 0;
     right: 0;
-
     top: 0;
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
       border-bottom: 1px solid $pt-high-contrast-mode-border-color;
     }
   }
-
   &.#{$ns}-position-bottom {
     @include react-transition-phase(
       "#{$ns}-overlay",
@@ -73,8 +65,8 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateY(0),
         ),
       ),
-      $pt-transition-duration * 2,
-      $pt-transition-ease,
+      calc(var(--bp-emphasis-transition-duration) * 2),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     @include react-transition-phase(
@@ -86,20 +78,19 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateY(0),
         ),
       ),
-      $pt-transition-duration,
+      var(--bp-emphasis-transition-duration),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     bottom: 0;
-    height: $drawer-default-size;
+    height: $drawer-default-size-next;
     left: 0;
-
     right: 0;
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
       border-top: 1px solid $pt-high-contrast-mode-border-color;
     }
   }
-
   &.#{$ns}-position-left {
     @include react-transition-phase(
       "#{$ns}-overlay",
@@ -110,8 +101,8 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateX(0),
         ),
       ),
-      $pt-transition-duration * 2,
-      $pt-transition-ease,
+      calc(var(--bp-emphasis-transition-duration) * 2),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     @include react-transition-phase(
@@ -123,20 +114,19 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateX(0),
         ),
       ),
-      $pt-transition-duration,
+      var(--bp-emphasis-transition-duration),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     bottom: 0;
     left: 0;
-
     top: 0;
-    width: $drawer-default-size;
+    width: $drawer-default-size-next;
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
       border-right: 1px solid $pt-high-contrast-mode-border-color;
     }
   }
-
   &.#{$ns}-position-right {
     @include react-transition-phase(
       "#{$ns}-overlay",
@@ -147,8 +137,8 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateX(0),
         ),
       ),
-      $pt-transition-duration * 2,
-      $pt-transition-ease,
+      calc(var(--bp-emphasis-transition-duration) * 2),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     @include react-transition-phase(
@@ -160,50 +150,41 @@ $dark-drawer-background-color: $dark-gray3 !default;
           translateX(0),
         ),
       ),
-      $pt-transition-duration,
+      var(--bp-emphasis-transition-duration),
+      var(--bp-emphasis-ease-default),
       $before: "&"
     );
     bottom: 0;
     right: 0;
-
     top: 0;
-    width: $drawer-default-size;
+    width: $drawer-default-size-next;
 
     @media (forced-colors: active) and (prefers-color-scheme: dark) {
       border-left: 1px solid $pt-high-contrast-mode-border-color;
     }
   }
-
-  &.#{$ns}-dark,
-  .#{$ns}-dark & {
-    background: $dark-drawer-background-color;
-    box-shadow: $pt-dark-dialog-box-shadow;
-    color: $pt-dark-text-color;
-  }
 }
 
-.#{$ns}-drawer-header:not(:where(.bp-next .#{$ns}-drawer-header)) {
+.#{$ns}-drawer-header {
   align-items: center;
   border-radius: 0;
-  box-shadow: 0 1px 0 $pt-divider-black;
+  box-shadow: 0 var(--bp-surface-border-width) 0 var(--bp-surface-divider-color-default);
   display: flex;
   flex: 0 0 auto;
-  min-height: $pt-icon-size-large + $drawer-padding;
-  padding: $drawer-padding * 0.25;
-  padding-left: $drawer-padding;
+  min-height: calc($pt-icon-size-large-next + $drawer-padding-next);
+  padding: calc($drawer-padding-next * 0.25);
+  padding-left: $drawer-padding-next;
   position: relative;
 
   @media (forced-colors: active) and (prefers-color-scheme: dark) {
     border-bottom: 1px solid $pt-high-contrast-mode-border-color;
   }
-
   .#{$ns}-icon-large,
   .#{$ns}-icon {
-    color: $pt-icon-color;
+    color: var(--bp-typography-color-base);
     flex: 0 0 auto;
-    margin-right: $drawer-padding * 0.5;
+    margin-right: calc($drawer-padding-next * 0.5);
   }
-
   .#{$ns}-heading {
     @include overflow-ellipsis;
     flex: 1 1 auto;
@@ -211,33 +192,18 @@ $dark-drawer-background-color: $dark-gray3 !default;
     margin: 0;
 
     &:last-child {
-      margin-right: $drawer-padding;
-    }
-  }
-
-  .#{$ns}-dark & {
-    box-shadow: 0 1px 0 $pt-dark-divider-black;
-
-    .#{$ns}-icon-large,
-    .#{$ns}-icon {
-      color: $pt-dark-icon-color;
+      margin-right: $drawer-padding-next;
     }
   }
 }
-
-.#{$ns}-drawer-body:not(:where(.bp-next .#{$ns}-drawer-body)) {
+.#{$ns}-drawer-body {
   flex: 1 1 auto;
-  line-height: $pt-spacing * 4.5;
+  line-height: calc(var(--bp-surface-spacing) * 4.5);
   overflow: auto;
 }
-
-.#{$ns}-drawer-footer:not(:where(.bp-next .#{$ns}-drawer-footer)) {
-  box-shadow: inset 0 1px 0 $pt-divider-black;
+.#{$ns}-drawer-footer {
+  box-shadow: inset 0 var(--bp-surface-border-width) 0 var(--bp-surface-divider-color-default);
   flex: 0 0 auto;
-  padding: $drawer-padding * 0.5 $drawer-padding;
+  padding: calc($drawer-padding-next * 0.5) $drawer-padding-next;
   position: relative;
-
-  .#{$ns}-dark & {
-    box-shadow: inset 0 1px 0 $pt-dark-divider-black;
-  }
 }
