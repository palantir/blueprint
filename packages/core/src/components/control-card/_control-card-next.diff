diff --git a/packages/core/src/components/control-card/_control-card.scss b/packages/core/src/components/control-card/_control-card-next.scss
index 826a7730c..22017016a 100644
--- a/packages/core/src/components/control-card/_control-card.scss
+++ b/packages/core/src/components/control-card/_control-card-next.scss
@@ -1,10 +1,10 @@
-// Copyright 2023 Palantir Technologies, Inc. All rights reserved.
+// Copyright 2026 Palantir Technologies, Inc. All rights reserved.
 // Licensed under the Apache License, Version 2.0.
 
-@import "../card/card-variables";
+@import "../card/card-variables-next";
 
 // use an extra selector to increase specificity
-.#{$ns}-card.#{$ns}-control-card:not(:where(.bp-next .#{$ns}-control-card)) {
+.#{$ns}-card.#{$ns}-control-card {
   &,
   .#{$ns}-card-list > & {
     // min-height & padding will be set on the label element so that it can take up the full size of the card and
@@ -14,16 +14,16 @@
   }
 }
 
-.#{$ns}-control-card:not(:where(.bp-next .#{$ns}-control-card)) {
+.#{$ns}-control-card {
   // need a lot of specificity here to override control styles for (switch, checkbox, etc.) and (align-left, align-right)
   // N.B. this is more space-efficient (in terms of generated CSS) than listing multiple compound selectors
   .#{$ns}-control.#{$ns}-control.#{$ns}-control {
     // control indicators should be top-aligned
     align-items: flex-start;
     display: flex;
-    gap: $pt-spacing * 2;
+    gap: calc(var(--bp-surface-spacing) * 2);
     margin: 0;
-    padding: $card-padding;
+    padding: $card-padding-next;
     width: calc(100%);
 
     &.#{$ns}-align-left {
@@ -37,11 +37,11 @@
     }
 
     .#{$ns}-card-list & {
-      padding: $card-padding;
+      padding: $card-padding-next;
     }
 
     .#{$ns}-card-list.#{$ns}-compact & {
-      padding: $card-padding-compact;
+      padding: $card-padding-compact-next;
     }
 
     .#{$ns}-control-indicator {
@@ -50,6 +50,6 @@
   }
 
   &.#{$ns}-compact .#{$ns}-control.#{$ns}-control.#{$ns}-control {
-    padding: $card-padding-compact;
+    padding: $card-padding-compact-next;
   }
 }
