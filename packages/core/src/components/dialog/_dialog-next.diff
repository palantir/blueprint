diff --git a/packages/core/src/components/dialog/_dialog.scss b/packages/core/src/components/dialog/_dialog-next.scss
index 083430d27..3cf10244f 100644
--- a/packages/core/src/components/dialog/_dialog.scss
+++ b/packages/core/src/components/dialog/_dialog-next.scss
@@ -1,19 +1,19 @@
-// Copyright 2015 Palantir Technologies, Inc. All rights reserved.
+// Copyright 2026 Palantir Technologies, Inc. All rights reserved.
 // Licensed under the Apache License, Version 2.0.
 
-@use "sass:math";
-@import "@blueprintjs/icons/lib/scss/variables";
 @import "../../common/mixins";
 @import "../../common/react-transition";
-@import "../../common/variables";
+@import "../../common/variables-next";
 
-$dialog-background-color: $light-gray5 !default;
-$dialog-border-radius: $pt-border-radius !default;
-$dialog-margin: ($pt-spacing * 8) 0 !default;
-$dialog-padding: $pt-spacing * 4 !default;
+// These component-local aliases remain here, as in BP6. The dialog partials imported after this file reuse them while their
+// CSS custom-property values remain runtime-configurable.
+$dialog-background-color-next: var(--bp-surface-background-color-base-rest);
+$dialog-border-radius-next: var(--bp-surface-border-radius);
+$dialog-margin-next: calc(var(--bp-surface-spacing) * 8) 0;
+$dialog-padding-next: calc(var(--bp-surface-spacing) * 4);
 
-.#{$ns}-dialog-container:not(:where(.bp-next .#{$ns}-dialog-container)) {
-  $dialog-transition-props: (
+.#{$ns}-dialog-container {
+  $dialog-transition-props-next: (
     opacity: (
       0,
       1,
@@ -24,11 +24,12 @@ $dialog-padding: $pt-spacing * 4 !default;
     ),
   );
 
+  // Preserve BP6's three-times-base dialog transition duration.
   @include react-transition(
     "#{$ns}-overlay",
-    $dialog-transition-props,
-    $duration: $pt-transition-duration * 3,
-    $easing: $pt-transition-ease-bounce,
+    $dialog-transition-props-next,
+    calc(var(--bp-emphasis-transition-duration) * 3),
+    var(--bp-emphasis-ease-bounce),
     $before: "&",
     $after: "> .#{$ns}-dialog"
   );
@@ -41,81 +42,63 @@ $dialog-padding: $pt-spacing * 4 !default;
   width: 100%;
 }
 
-.#{$ns}-dialog:not(:where(.bp-next .#{$ns}-dialog)) {
-  background: $dialog-background-color;
-  border-radius: $dialog-border-radius;
-  box-shadow: $pt-dialog-box-shadow;
+.#{$ns}-dialog {
+  background: $dialog-background-color-next;
+  border-radius: $dialog-border-radius-next;
+  box-shadow: var(--bp-surface-shadow-3);
+  color: var(--bp-typography-color-base);
   display: flex;
   flex-direction: column;
-  margin: $dialog-margin;
+  margin: $dialog-margin-next;
   pointer-events: all;
   user-select: text;
-  width: $pt-spacing * 125;
+  width: calc(var(--bp-surface-spacing) * 125);
 
   &:focus {
     outline: 0;
   }
 
-  &.#{$ns}-dark,
-  .#{$ns}-dark & {
-    background: $pt-dark-app-background-color;
-    box-shadow: $pt-dark-dialog-box-shadow;
-    color: $pt-dark-text-color;
-  }
-
   @media (forced-colors: active) and (prefers-color-scheme: dark) {
     border: 1px solid $pt-high-contrast-mode-border-color;
   }
 }
 
-$dialog-header-padding: $pt-spacing;
+$dialog-header-padding-next: var(--bp-surface-spacing);
 
-.#{$ns}-dialog-header:not(:where(.bp-next .#{$ns}-dialog-header)) {
+.#{$ns}-dialog-header {
   align-items: center;
-  background: $white;
-  border-radius: $dialog-border-radius $dialog-border-radius 0 0;
-  box-shadow: 0 1px 0 $pt-divider-black;
+  background: var(--bp-surface-background-color-base-rest);
+  border-radius: $dialog-border-radius-next $dialog-border-radius-next 0 0;
+  box-shadow: 0 1px 0 var(--bp-surface-divider-color-default);
   display: flex;
   flex: 0 0 auto;
-  min-height: $pt-button-height + $dialog-header-padding * 2;
-  padding: $dialog-header-padding;
-  padding-left: $dialog-padding;
+  min-height: calc($pt-input-height-next + ($dialog-header-padding-next * 2));
+  padding: $dialog-header-padding-next;
+  padding-left: $dialog-padding-next;
   z-index: 0;
 
   .#{$ns}-icon-large,
   .#{$ns}-icon {
     flex: 0 0 auto;
-    margin-left: -$pt-spacing;
-    margin-right: $dialog-padding * 0.5;
+    margin-left: calc(-1 * var(--bp-surface-spacing));
+    margin-right: calc($dialog-padding-next * 0.5);
 
-    // only apply light color to icons that are not intent-specific
+    // only apply the neutral color to icons that are not intent-specific
     &:not([class*="#{$ns}-intent"]) {
-      color: $pt-icon-color;
+      // BP6's default icon color was the muted text color; the neutral typography role is its BP7 counterpart.
+      color: var(--bp-typography-color-intent-neutral);
     }
   }
 
   .#{$ns}-heading {
     @include overflow-ellipsis;
     flex: 1 1 auto;
-    font-size: 14px;
+    font-size: var(--bp-typography-size-lg);
     line-height: inherit;
     margin: 0;
 
     &:last-child {
-      margin-right: $dialog-padding;
-    }
-  }
-
-  .#{$ns}-dark & {
-    background: $dark-gray3;
-    box-shadow: inset 0 0 0 1px $pt-dark-divider-white;
-
-    // only apply dark color to icons that are not intent-specific
-    :not([class*="#{$ns}-intent"]) {
-      &.#{$ns}-icon-large,
-      &.#{$ns}-icon {
-        color: $pt-dark-icon-color;
-      }
+      margin-right: $dialog-padding-next;
     }
   }
 
