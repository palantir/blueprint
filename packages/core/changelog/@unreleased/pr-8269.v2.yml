type: fix
fix:
  description: '`useHotkeys`: `stopPropagation` now prevents a local hotkey from also
    being handled by an enclosing context'
  links:
  - https://github.com/palantir/blueprint/pull/8269
